[pytest]
addopts = 
    --strict-markers
    --disable-warnings
    -ra
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: marks tests as unit tests (fast, no database)

asyncio_mode = auto 